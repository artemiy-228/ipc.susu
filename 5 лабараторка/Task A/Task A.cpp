#include iostream

using namespace std;

int main(){
   int n;
   cin  n;
   double k, m, sum;
   for(k = 1; k = n; k++){
      for(m = 1; m = n;m++){
      sum += n  (2  k + m);
      }
   }
   cout.precision(6);
   cout  fixed  sum;
}